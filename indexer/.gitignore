# Envio codegen output — regenerated from config.yaml + schema.graphql via `pnpm codegen`.
generated/
# Local state (Envio dev creates these when streaming chain history)
.envio/
.data/
.docker/

# Standard JS junk
node_modules/
dist/
*.log
.DS_Store
package-lock.json

# Per-session config generated by scripts/start-indexer.sh — patched start_block aligned
# to the running sidecar; safe to regenerate on every launch.
config.runtime.yaml
